export type FacultyRequestError = string | null;
// If FacultyRequestFormData is not already exported from validation, you can define it here for clarity:
// export interface FacultyRequestFormData { ... } 